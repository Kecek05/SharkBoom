using System.Threading.Tasks;
using UnityEngine;

public class ClientGameManager //Actual Logic to interact with UGS (Relay, Lobby, etc)
{
    private NetworkClient networkClient;

    public async Task InitAsync()
    {
        //Authenticate Player
    }
}
